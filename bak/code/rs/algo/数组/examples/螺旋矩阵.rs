fn main() {
    //
}

fn solve(n: usize) -> Vec<Vec<usize>> {
    //
    let res = vec![vec![0; n]; n];
    let mut count = 0;
    // loop {

    //     for i in
    // }

    res
}

#[cfg(test)]
mod test {
    use crate::solve;

    #[test]
    fn it_dhould_return_ok() {
        assert_eq!(solve(3), vec![vec![1, 2, 3]], vec![8, 9, 4,], vec![7, 6, 5]);
    }
    #[test]
    fn it_should_return_err() {
        // assert_eq!(solve(&mut vec![1, 2, 3, 4, 7, 9, 10], 2), 6);
    }
}
